import pandas as pd
import networkx as nx
from typing import Tuple, Dict


def run(data: pd.DataFrame, **kwargs) -> Tuple[nx.DiGraph, Dict[str, object]]:
    raise RuntimeError("COSMO is not installed")
