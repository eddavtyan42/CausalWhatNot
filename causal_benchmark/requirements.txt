#################################################################
# Core benchmark dependencies (pinned for reproducibility)
# Python 3.10 is recommended. NOTEARS (causalnex backend) requires:
#   * Python < 3.11
#   * networkx < 3 (causalnex 0.12.x constraint)
#################################################################
causal-learn==0.1.3.6
networkx==2.8.8        # causalnex 0.12.x requires <3
pandas==1.5.3
numpy==1.23.5
pyyaml==6.0
joblib==1.3.2

# NOTEARS / CausalNex (optional). Pin exact version for determinism.
causalnex==0.12.0; python_version < '3.11'

# scikit-learn 1.2.2 is the latest verified with Python 3.10 + these pins.
scikit-learn==1.2.2

# Test dependencies
pytest==8.3.5
pytest-timeout

# (PyTorch will be pulled in automatically when installing causalnex.)
